# Local mode (default: on) — full upload/download without coordinator or storage hosts
# CERA_LOCAL_MODE=1

# Production: talk to real API and ZMQ storage nodes
# CERA_LOCAL_MODE=0
# CERA=http://localhost:5000/
